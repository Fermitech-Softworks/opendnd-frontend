{
  "private": true,
  "name": "opendnd-frontend",
  "version": "0.2.0",
  "license": "MIT",
  "scripts": {
    "build": "preact build",
    "serve": "sirv build --cors --single",
    "dev": "preact watch"
  },
  "devDependencies": {
    "preact-cli": "^3.4.1",
    "sirv-cli": "^0.4.5"
  },
  "dependencies": {
    "bootstrap": "^4.4.1",
    "history": "^4.10.1",
    "preact": "^10.1.0",
    "preact-render-to-string": "^5.1.2",
    "preact-router": "^3.2.1",
    "react-bootstrap": "^1.0.1"
  }
}
